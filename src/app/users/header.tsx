import { DarkmodeButton } from "@/utilities/darkmode_button"

export const Header = () => {
  return (
    <header className="bg-red-500 dark:bg-green-500">
      Header of Users
      <DarkmodeButton />
      hehe
    </header>
  )
}